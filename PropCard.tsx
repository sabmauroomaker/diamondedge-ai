import Link from 'next/link'
import StatPill from './StatPill'
export default function PropCard({p}:{p:any}){return <Link href={`/props/${p.id}`} className="block glass rounded-3xl p-4 hover:border-purple/60 transition"><div className="flex items-start gap-3"><div className="h-12 w-12 rounded-full bg-gradient-to-br from-purple to-emerald-500 grid place-items-center text-xs font-black">{p.avatar}</div><div className="flex-1"><div className="flex justify-between gap-3"><div><h3 className="font-bold text-lg">{p.player} <span className="text-zinc-500">({p.hand})</span></h3><p className="text-sm text-zinc-400">{p.game}</p></div><StatPill type={p.edge==='Favorable'?'good':'neutral'}>{p.edge}</StatPill></div><div className="mt-4 flex items-center justify-between"><div><p className="text-zinc-400 text-sm">{p.market}</p><p className="text-2xl font-black">{p.line} <span className="text-purple text-base">{p.odds}</span></p></div><div className="text-right"><p className="text-3xl font-black text-red-400">{p.hit}</p><p className="text-xs text-zinc-500">L10 hit rate</p></div></div><p className="mt-3 text-sm text-zinc-300 leading-relaxed">{p.notes}</p></div></div></Link>}
